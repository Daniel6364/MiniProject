<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserMapper10">

	<resultMap id="userSelectMap" type="com.model2.mvc.service.User">
		<result property="userId" column="user_id" jdbcType="VARCHAR2" />
		<result property="userName" column="user_name" jdbcType="VARCHAR2" />
		<result property="password" column="password" jdbcType="VARCHAR2" />
		<result property="role" column="role" jdbcType="VARCHAR2" />
		
		<result property="ssn" column="ssn" jdbcType="VARCHAR2" />
		<result property="cellPhone" column="cell_phone" jdbcType="VARCHAR2" />
		<result property="addr" column="addr" jdbcType="VARCHAR2" />
		<result property="email" column="email" jdbcType="VARCHAR2" />
		<result property="regDate" column="reg_date" jdbcType="DATE" />
	</resultMap>

	<insert id="insertUser" parameterType="com.model2.mvc.service.User">
		INSERT
		INTO users( user_id, user_name, password, role, ssn, cell_phone, addr, email, reg_date)
		VALUES ( #{userId},	#{userName}, #{password}, 'user', 
				 #{ssn}, #{cellPhone}, #{addr}, #{email}, #{regDate:DATE})
	</insert>

	<select id="findUser" parameterType="string" resultMap="userSelectMap">
		SELECT
		user_id, user_name, password, role, ssn, cell_phone, addr, email, reg_date
		FROM users
		WHERE user_id = #{value}
	</select>

<!--  
	<update id="updateUser" parameterType="mybatis.service.domain.User">
		UPDATE users
		<set>
			<if test="userName != null">user_name = #{userName},</if>
			<if test="password != null">password = #{password},</if>
			age = #{age:INTEGER},
			grade =
			<if test="grade==0">null</if>
			<if test="grade!=0">#{grade}</if>
		</set>
		WHERE user_id = #{userId}
	</update>

	<delete id="removeUser" parameterType="string">
		DELETE
		FROM users
		WHERE user_id = #{value}
	</delete>

	<sql id="select-users">
		SELECT
		user_id, user_name, password, age, grade, reg_date
		FROM users
	</sql>

	<sql id="orderby-userId-desc">
		ORDER BY user_id DESC
	</sql>

	<select id="getUserList" parameterType="mybatis.service.domain.Search" resultMap="userSelectMap">
		<include refid="select-users"/>
		<where>
			<choose>
				<when test="searchCondition == 'userId'">
					<if test="userId.size != 0">
						user_id IN
						<foreach item="value" collection="userId" open="(" separator="," close=")">
							#{value}
						</foreach>
					</if>
				</when>
				<when test="searchCondition == 'userName'">
					<if test="userName[0] != null">
						user_name IN
						<foreach item="value" collection="userName" open="(" separator="," close=")">
							#{value}
						</foreach>
					</if>
				</when>
			</choose>
-->
	<!--  <if test="searchCondition == 'userId'">
		<if test="userId.size != 0">
			user_id IN
			<foreach item="value" collection="userId" open="(" separator="," close=")">
				#{value}
			</foreach>
		</if>
	</if>
	<if test="searchCondition == 'userName'">
		<if test="userName[0] != null">
			user_name IN
			<foreach item="value" collection="userName" open="(" separator="," close=")">
				#{value}
			</foreach>
		</if>
	</if> -->
			
<!-- 			
		</where>
		<include refid="orderby-userId-desc" />
	</select>
-->
</mapper>